{
  "name": "fuel-planner",
  "version": "1.0.0",
  "description": "Fuel Planner - Plan your fuel stops efficiently",
  "main": "app.js",
  "scripts": {
    "build": "node build.js",
    "start": "python -m http.server 8080",
    "ios:build": "npm run build && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:run": "npm run build && npx cap sync ios && npx cap open ios"
  },
  "keywords": [
    "fuel",
    "planner",
    "trucking",
    "ios"
  ],
  "author": "Fuel Planner Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/ios": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
